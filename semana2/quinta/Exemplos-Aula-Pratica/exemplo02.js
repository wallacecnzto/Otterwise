let numbers = [5, 3, 2, 1, 0];
let soma = 0;

for (let index = 0; index < numbers.length; index++) {
  soma = soma + numbers[index];
}

// soma -> undefined ->
// numbers -> [5, 3, 2, 1, 0]
// index -> 0
